# Examples

Runnable programs that call the real API, kept out of the published package.

Nothing here yet. The first one will need a `CLOUDFLARE_ACCOUNT_ID` and a `CLOUDFLARE_API_TOKEN`
with `Workers AI - Read` and `Workers AI - Edit`, and will double as the check that settles the open
questions recorded in [`../docs/jev-api.md`](../docs/jev-api.md).
